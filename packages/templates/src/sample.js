// @TODO To be updated with user configuration
const config = {
    framework: 'nextjs/app-router',
  };

  function getFramework() {
    return config.framework;
  }

  module.exports = {
    getFramework,
  };
